const storeIntoDb = require('./../../../../src/utils/books/storeDetails');
const model = require('./../../../../models');

describe('storeIntoDb ():', () => {
  beforeAll();
  it('should store the details into table books in db', () => {

  });
});
